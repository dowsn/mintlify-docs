---
title: 'MY_Controller_Lingual'
description: 'A trait containing utility functions for linguistic manipulations and formatting'
---

This trait provides various utility functions for manipulating strings, handling pluralization, formatting dates, and managing language settings.

## String Manipulation

### getPrettyName

Converts a table name to a more readable format.

<CodeGroup>

```php
public static function getPrettyName($name)
```

</CodeGroup>

<Param name="name" type="string">
  The table name to be converted
</Param>

Returns a string with the first letter capitalized and underscores replaced by spaces.

### getPrettyNameInAllCaps

Converts a table name to an all-uppercase format.

<CodeGroup>

```php
public function getPrettyNameInAllCaps($name)
```

</CodeGroup>

<Param name="name" type="string">
  The table name to be converted
</Param>

Returns a string with all letters in uppercase and underscores replaced by spaces.

### singularizeName

Converts a plural name to its singular form.

<CodeGroup>

```php
public function singularizeName($name)
```

</CodeGroup>

<Param name="name" type="string">
  The plural name to be singularized
</Param>

Returns the singular form of the given name, with exceptions for irregular nouns.

### pluralizeName

Converts a singular name to its plural form.

<CodeGroup>

```php
public static function pluralizeName($name)
```

</CodeGroup>

<Param name="name" type="string">
  The singular name to be pluralized
</Param>

Returns the plural form of the given name, with exceptions for irregular nouns.

### nullToString

Converts null or undefined values to an empty string.

<CodeGroup>

```php
public function nullToString($value)
```

</CodeGroup>

<Param name="value" type="mixed">
  The value to be checked and converted if necessary
</Param>

Returns an empty string if the input is null, undefined, or an empty value; otherwise, returns the input value.

### remove_accents

Removes accents from a string.

<CodeGroup>

```php
function remove_accents($string)
```

</CodeGroup>

<Param name="string" type="string">
  The string from which to remove accents
</Param>

Returns the input string with accented characters replaced by their non-accented equivalents.

## Data Formatting

### getSelector

Generates an array for use in select dropdowns.

<CodeGroup>

```php
public function getSelector($table_name, $main_value = 'name', $value_in_brackets = false, $defaultValue = 'select')
```

</CodeGroup>

<Param name="table_name" type="string">
  The name of the table to fetch data from
</Param>

<Param name="main_value" type="string" default="name">
  The field to use as the main value in the dropdown
</Param>

<Param name="value_in_brackets" type="string|false" default="false">
  An optional field to display in brackets after the main value
</Param>

<Param name="defaultValue" type="string" default="select">
  The type of default value to include ('select', 'all', or none)
</Param>

Returns an array of key-value pairs suitable for use in a dropdown selector.

### dateFormatter

Formats a date string according to the specified format and locale.

<CodeGroup>

```php
public function dateFormatter($date, $format)
```

</CodeGroup>

<Param name="date" type="string">
  The date string to be formatted
</Param>

<Param name="format" type="string">
  The desired output format
</Param>

Returns the formatted date string based on the current language setting.

## Language Management

### _setLang

Sets the language for the application based on a cookie value.

<CodeGroup>

```php
protected function _setLang()
```

</CodeGroup>

This method checks for a language cookie and sets the application language accordingly. It also loads the appropriate language file and sets the locale.

<Note>
This method is protected and intended to be used internally within the class.
</Note>