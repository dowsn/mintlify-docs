Setting up new project

empty tables, create backend_user

define modules

start creating views

adjust detail pages how they should look probably every project different

prepare content table, delete unneeded, add columns and tables in phpmyadmin

search for // treatstart comment to set db and other general settings

data preparation from

and inside function for every type
use getArticleInfo
use getNoarticleInfo
specify types in constants

teaser getter
module image getter

adjust config, logos, database connection


define module keys

style and create static pages
